{
  "name": "leadintel-dashboard",
  "version": "0.2.0",
  "private": true,
  "description": "LeadIntel client portal — static HTML5 (no React)",
  "scripts": {
    "start": "python -m http.server 3000 --directory public"
  }
}
