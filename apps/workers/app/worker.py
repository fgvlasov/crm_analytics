"""Workers package — Phase 3+ jobs run via apps/api worker_loop (see docker-compose)."""

from __future__ import annotations

print(
    "Use: python -m app.scripts.worker_loop from the API package "
    "(docker compose workers service)."
)
